{
  "name": "gies-portfolio-workspace",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "npm --prefix artifacts/portfolio run dev",
    "build": "npm --prefix artifacts/portfolio run build",
    "preview": "npm --prefix artifacts/portfolio run preview",
    "typecheck": "npm --prefix artifacts/portfolio run typecheck"
  }
}
